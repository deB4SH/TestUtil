image: maven:latest

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - .m2cache/repository

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2cache/repository"

stages:
  - build

compile:
  stage: build
  tags:
    - homelab
  artifacts:
    paths:
        - target/*.jar
  script:
    - printenv
    - mvn clean install -s .m2/settings.xml
    - mvn deploy -s .m2/settings.xml -e -X